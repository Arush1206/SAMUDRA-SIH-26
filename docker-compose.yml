version: "3.9"

services:
  backend:
    build: ./backend
    container_name: samudra-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend/data:/app/data
    restart: unless-stopped
